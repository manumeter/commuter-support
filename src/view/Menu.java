package ch.zhaw.mas8i.pendlersupport.view;

/**
 * This is a UI component, used for the configuration menu.
 * The run method is used to start the menu. The run method
 * may get started directly instead of in a new thread if
 * the application should wait until the menu gets closed
 * (e.g. for console menu).
 */
interface Menu extends Runnable {
	
}
